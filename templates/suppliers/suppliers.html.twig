{%  extends 'base.html.twig'%}

{% block title %} Symfony tech test {% endblock %}

{% block body %}

{% if app.user %}
    <h1 style="font-size: 19px; display:flex; Justify-content: center;" >Welcome! thanks for taking your time to create an account. Feel free to explore and create new data(suppliers) if you have none or insufficient
    <a class="pl-2" href="/create">here</a>
    </h1>
{% else %}
    <h1 style="font-size: 19px; display:flex; Justify-content: center" >Welcome! to fully enjoy your user experience, you can create an account. <br>Also, Feel free to explore and create new data(suppliers) if you have none or insufficient
    {# <a class="pl-2" href="/create">here</a> #}
    </h1>
{% endif %}

{% for supplier in suppliers %}
<div>
       <div class="container mt-5 col" style="max-width: fit-content">
            <div class="row">
                <div>
                    <div class="card p-3">
                        <div class="mb-3">
                            <div class="d-flex flex-column mb-1 font-weight-bold"><span>{{supplier.name}}</span>
                            <div class="d-flex flex-column mb-1"><span>{{supplier.email}}</span>
                            <div class="d-flex flex-column mb-1"><span>{{supplier.phone}}</span>
                        </div>
                        <h6>Get more context on your users with stripe data inside our platform.</h6>
                        <div class="d-flex justify-content-between align-items-center install mt-3 mb-3">
                            <span class="p-1 btn-warning">{{supplier.type}}</span>
                            {% if supplier.status == true %}
                                <span style="color:#2ECC71">active <img style="width:15px; height:15px" src="https://img.icons8.com/fluency-systems-filled/48/40C057/ok--v1.png"/></span>
                            {% endif %}
                             {% if supplier.status == false %}
                                <span style="color: #adafae">inactive <img style="width:15px; height:15px" src="https://img.icons8.com/windows/32/000000/active-state.png"/></span>
                            {% endif %}
                        </div>
                        <div class=" buttons d-flex flex-row ml-0">
                            <button><a href="/delete/{{ supplier.id }}" >Delete</a></button>
                            <button><a href="/create" >Create</a></button>
                            <button><a href="/edit/{{ supplier.id }}" >Edit</a></button>
                        </div>    
                    </div>
                </div>
            </div>
        </div>
{% endfor %}
{% for message in app.flashes('notice') %}
   <div class="alert alert-success">
   {{message}}
   </div>
{% endfor %}

{% endblock %}
{% block javascripts %}
  <script src="/js/supplier.js"></script>
{% endblock %}


{# <div>
       <div class="container mt-5">
            <div class="row">
                <div class="col-md-6">
                    <div class="card p-3">
                        <div class="d-flex flex-row mb-3">
                            <div class="d-flex flex-column mb-1"><span>{{supplier.name}}</span>
                            <div class="d-flex flex-column mb-1"><span>{{supplier.email}}</span>
                            <div class="d-flex flex-column mb-1"><span>{{supplier.phone}}</span>
                        </div>
                        <h6>Get more context on your users with stripe data inside our platform.</h6>
                        <div class="d-flex justify-content-between install mt-3"><span>Installed 172 times</span><span class="text-primary">View&nbsp;<i class="fa fa-angle-right"></i></span></div>
                        <button><a href="/delete/{{ supplier.id }}" >Delete</a></button>
                        <button><a href="/create" >Create</a></button>
                        <button><a href="/edit/{{ supplier.id }}" >Edit</a></button>
                    </div>
                </div>
            </div>
        </div> #}